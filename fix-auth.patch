--- src/components/Auth.tsx
+++ src/components/Auth.tsx
@@ -56,6 +56,12 @@
         }),
       });
 
+      const text = await res.text();
+      let data;
+      try { data = JSON.parse(text); } catch (e) {
+        throw new Error("Backend not running. If on Vercel, the Express server isn't deployed.");
+      }
+
       const data = await res.json();
